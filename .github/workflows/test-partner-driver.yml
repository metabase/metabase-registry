name: Test Partner Driver

on: push

jobs:
  clickhouse:
    runs-on: ubuntu-latest
    steps:
      - name: Test Athena driver
        uses: ./.github/actions/test-partner
        with:
          DRIVER: 'clickhouse'
